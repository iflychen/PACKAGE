# Python SPC service 的位置（uvicorn app.main:app 預設跑在這個位址）
# 複製這個檔成 .env.local 即可覆寫
SPC_API_BASE=http://127.0.0.1:8000

# Neon PostgreSQL 連線字串（沒填 → 自動退回種子資料）
# 從 Neon Dashboard 右上「Connect」複製 postgresql://... 貼在這
DATABASE_URL=
